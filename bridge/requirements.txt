bleak>=1.0
websockets>=15.0
pycryptodome>=3.20
cryptography>=48.0.1  # geração do certificado TLS autoassinado (opt-in, ver CAREWEAR_WS_TLS em ble_bridge.py); DEP-001: piso subido por GHSA-r6ph-v2qm-q3c2, GHSA-p423-j2cm-9vmq, GHSA-537c-gmf6-5ccf, GHSA-m959-cc7f-wv43
sqlalchemy>=2.0.0  # ORM — storage_advanced.py, 2026-07-26: deixou de ser opcional (dual-write removido); é agora a única base de dados do bridge
argon2-cffi>=23.1.0  # derivação de chave (Argon2id) para crypto_utils.py, importado incondicionalmente por storage_advanced.py — mesma razão acima
